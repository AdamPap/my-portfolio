import { ArrowRight as Arrow } from "react-bootstrap-icons";
import styled from "styled-components";

const StyledArrow = styled(Arrow)`
  margin-left: 10px;
  margin-right: 10px;
  transition: all 0.3s ease-in-out;
`;

export default StyledArrow;
